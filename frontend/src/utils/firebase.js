// Firebase Utility - Re-export backend services for easier imports
// This file provides a convenient way to import Firebase services in frontend components

// Re-export everything from backend
export * from '../../../backend/index.js';

// You can also import specific services like this:
// import { auth, db, storage, loginUser, registerStudent } from './utils/firebase';
